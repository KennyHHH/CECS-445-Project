@model IEnumerable<WebApplication1.Models.Adopt>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.APetName)
        </th>
       
        <th>
            @Html.DisplayNameFor(model => model.APetDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.APetName)
        </td>
     
        <td>
            @Html.DisplayFor(modelItem => item.APetDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdoptId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdoptId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdoptId })
        </td>
    </tr>
}

</table>
